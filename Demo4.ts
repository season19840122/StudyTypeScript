// type inference
// type annotation

// type inference
const one = 1;
const two = 2;
const three = one + two;

// type annotation
function getTotal(one: number, two: number) {
  return one + two;
}

const total = getTotal(1, 2);

const XiaoJieJie = {
  name: "刘英",
  age: 18,
};
